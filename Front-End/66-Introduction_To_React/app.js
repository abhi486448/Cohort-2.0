// var user = "Sarthak";

const king = {
    name: "virat",
    age: 34,
    inForm: true
};

const  quene = {
    name: "sriti",
    age: 25,
    inForm: true
};

export default king;

// export default quene; // it can only export one thing , we can not export king and quene at same time with 'export default'